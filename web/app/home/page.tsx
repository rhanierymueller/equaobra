import HomeClient from './HomeClient'

export const metadata = {
  title: 'Profissionais próximos — EquaObra',
  description: 'Encontre profissionais qualificados para sua obra.',
}

export default function HomeRoute() {
  return <HomeClient />
}
